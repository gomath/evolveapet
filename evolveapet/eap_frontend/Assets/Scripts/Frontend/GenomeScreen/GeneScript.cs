using UnityEngine;
using System.Collections;
using System;

namespace EvolveAPet{
public class GeneScript : MonoBehaviour {

	public static Color guessedColor = Color.green; //new Color32(0,255,0,110);
	public static Color unknownColor = new Color32(255,0,255,110);
	public Color actualColor = new Color32(255,0,255,110);
	
	// Use this for initialization
	void Start () {
	}
	
	// Update is called once per frame
	void Update () {
		
	}

}
}
